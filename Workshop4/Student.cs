public class Student
{
    // Instance fields (unique for each object)
    public string Name;
    public int Age;
    public string Address;

    // Static field (shared by all objects)
    public static string CollegeName = "IIC College";
}
